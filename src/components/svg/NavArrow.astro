---
const { class: className, ...rest } = Astro.props
---

<svg class:list={[className]} {...rest} xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 24 24">
	<path fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="m17 14l-5-5l-5 5" />
</svg>

<style>
    svg {
		display: none;
        color: var(--color-nav-link);
    }
	.arrow-collapse {
		transform: rotate(90deg);
	}
	.arrow-expand {
		transform: rotate(180deg);
	}
	@media (min-width: 768px) {
		svg {
			display: inherit;
		}
	}
</style>
